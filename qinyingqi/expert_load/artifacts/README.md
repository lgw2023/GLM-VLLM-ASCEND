# Artifacts

This directory is intentionally kept empty in Git. Shared logs, responses, decoded `.npy` routes and manifests belong under `RUN_HOST_ROOT`; safety-critical leases, lifecycle locks, recovery state and container ownership records belong under node-local `LOCAL_STATE_ROOT`. Neither location is inside the repository.
